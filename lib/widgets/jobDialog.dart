import 'package:flutter/material.dart';
import 'package:team_game_job_board/models/model.dart';

class JobDialog extends StatelessWidget {
  const JobDialog({Key? key, this.ModelJob}) : super(key: key);
  final ModelJob;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
